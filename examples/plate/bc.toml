# =============================================================================
# Solver Boundary Conditions Configuration
# Auto-generated by Mesh Pipeline
#
# Total Boundary Patches: 7
# Total Global Boundary Faces: 12320
#
# Available BC Types (implemented):
#   - SLIP_WALL          (Inviscid slip wall; no parameters)
#   - SYMMETRY           (Planar symmetry; no parameters)
#   - SUPERSONIC_INLET   (p [Pa], t [K], mach, direction [x, y, z])
#   - SUPERSONIC_OUTLET  (Supersonic outflow extrapolation; no parameters)
#   - FARFIELD           (mach_inf, p_inf [Pa], t_inf [K], alpha [deg], beta [deg])
#
# Planned BC Types (not yet implemented by the solver):
#   - SUBSONIC_INLET     (p0 [Pa], t0 [K], direction)
#   - PRESSURE_OUTLET    (p_back [Pa])
#   - WALL_ADIABATIC     (No-slip viscous adiabatic wall)
#   - WALL_ISOTHERMAL    (t_wall [K])
# =============================================================================

[[boundary_condition]]
patch_id          = 0
name              = "SYMMETRY"
cgns_type         = "UserDefined"
global_face_count = 12000
type              = "SYMMETRY"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 1
name              = "INLET"
cgns_type         = "UserDefined"
global_face_count = 60
type              = "SUPERSONIC_INLET"

p = 5590.34
t = 107.14
velocity = [622.13, 0.0, 0.0]

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 2
name              = "OUTLET"
cgns_type         = "UserDefined"
global_face_count = 60
type              = "SUPERSONIC_OUTLET"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 3
name              = "TOP"
cgns_type         = "UserDefined"
global_face_count = 100
type              = "SUPERSONIC_OUTLET"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 4
name              = "INLET_SYMMETRY"
cgns_type         = "UserDefined"
global_face_count = 10
type              = "SYMMETRY"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 5
name              = "PLANE"
cgns_type         = "UserDefined"
global_face_count = 90
type              = "NO_SLIP_WALL_ADIABATIC"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

[[boundary_condition]]
patch_id          = 6
name              = "TOTAL_1_1"
cgns_type         = "UserDefined"
global_face_count = 0
type              = "SYMMETRY"

# [Pick a type above, then uncomment its parameters]
# p         = 101325.0        # SUPERSONIC_INLET: static pressure [Pa]
# t         = 300.0           # SUPERSONIC_INLET: static temperature [K]
# mach      = 2.0             # SUPERSONIC_INLET: Mach number [-]
# direction = [1.0, 0.0, 0.0] # SUPERSONIC_INLET: flow direction
# mach_inf  = 2.0             # FARFIELD: freestream Mach number
# p_inf     = 101325.0        # FARFIELD: static pressure [Pa]
# t_inf     = 300.0           # FARFIELD: static temperature [K]
# alpha     = 0.0             # FARFIELD: angle of attack [deg]
# beta      = 0.0             # FARFIELD: sideslip angle [deg]

# -----------------------------------------------------------------------------

